{
  "name": "surswipe",
  "version": "1.0.0",
  "description": "Mobile-only swipe survey - Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.4"
  }
}
